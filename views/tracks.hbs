{{#each tracks}}
  <div class='track'>
    <h2>{{#each artists}}{{name}}{{#unless @last}}, {{/unless}} {{/each}}<br />
      {{name}}</h2>
    <audio controls src='{{preview_url}}'></audio>
  </div>
{{/each}}